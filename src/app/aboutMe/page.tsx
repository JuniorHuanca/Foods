import Layout from "@/components/Layout/Layout";

type Props = {};

const AboutMe = (props: Props) => {
  return (
    <Layout>
      <div>AboutMe</div>
    </Layout>
  );
};

export default AboutMe;
